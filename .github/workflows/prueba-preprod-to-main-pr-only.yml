name: 🔄 Create PR pre_prod to main

on:
  workflow_dispatch:
    inputs:
      repositories:
        description: "Selecciona el repositorio para crear PR (pre_prod → main)"
        required: true
        type: choice
        options:
          - zend-library
          - zend-src
          - basket-zend-src
          - running-zend-src
          - front-land-bundle
          - back-land-bundle
          - atland
          - atland-vendor
          - superadmin
          # Agrega los repos de la organización aquí

jobs:
  create-pr-job:
    name: Create PR pre_prod → main
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Set variables
        id: vars
        run: |
          echo "REPO_NAME=${{ github.event.inputs.repositories }}" >> $GITHUB_ENV
          echo "CLONE_URL=https://x-access-token:${{ secrets.ORG_REPO_UPDATE_PAT }}@github.com/${{ github.repository_owner }}/${{ github.event.inputs.repositories }}.git" >> $GITHUB_ENV
          echo "PR_BRANCH=temp-preprod-to-main/$(date +%Y%m%d-%H%M%S)" >> $GITHUB_ENV

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/${{ github.event.inputs.repositories }}
          token: ${{ secrets.ORG_REPO_UPDATE_PAT }}
          ref: pre_prod
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Check if branches exist
        run: |
          MAIN_EXISTS=$(git ls-remote --heads origin main | wc -l)
          PREPROD_EXISTS=$(git ls-remote --heads origin pre_prod | wc -l)
          
          if [ "$MAIN_EXISTS" -eq 0 ]; then
            echo "❌ La rama 'main' no existe en el repositorio $REPO_NAME. Saliendo."
            exit 1
          fi
          
          if [ "$PREPROD_EXISTS" -eq 0 ]; then
            echo "❌ La rama 'pre_prod' no existe en el repositorio $REPO_NAME. Saliendo."
            exit 1
          fi
          
          echo "✅ Ambas ramas existen en el repositorio $REPO_NAME."

      - name: Check for differences and prepare branch
        id: check_diff
        run: |
          # Asegurar que tenemos todas las ramas y commits
          git fetch origin main:refs/remotes/origin/main
          git fetch origin pre_prod:refs/remotes/origin/pre_prod
          
          # Contar cuántos commits hay de diferencia
          COMMIT_COUNT=$(git rev-list --count origin/main..origin/pre_prod)
          echo "Número de commits diferentes entre main y pre_prod: $COMMIT_COUNT"
          
          if [ "$COMMIT_COUNT" -eq 0 ]; then
            echo "DIFF_EXISTS=false" >> $GITHUB_ENV
            echo "ℹ️ No hay diferencias entre main y pre_prod. No se necesita crear PR."
          else
            echo "DIFF_EXISTS=true" >> $GITHUB_ENV
            echo "✅ Se encontraron $COMMIT_COUNT commits para mergear de pre_prod a main. Creando PR..."
            
            # Crear rama temporal desde pre_prod
            git checkout -b ${{ env.PR_BRANCH }} origin/pre_prod
            
            # Verificar que la rama temporal tiene los commits correctos
            TEMP_COMMIT_COUNT=$(git rev-list --count origin/main..${{ env.PR_BRANCH }})
            echo "Commits en la rama temporal: $TEMP_COMMIT_COUNT"
            
            if [ "$TEMP_COMMIT_COUNT" -eq "$COMMIT_COUNT" ]; then
              echo "✅ Rama temporal creada correctamente con todos los commits"
              git push origin ${{ env.PR_BRANCH }}
            else
              echo "❌ Error: La rama temporal no tiene todos los commits necesarios"
              exit 1
            fi
          fi

      - name: Create pull request
        if: env.DIFF_EXISTS == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.ORG_REPO_UPDATE_PAT }}
          base: main
          branch: ${{ env.PR_BRANCH }}
          delete-branch: false
          title: "🔄 Merge pre_prod into main"
          body: |
            ## 📋 Descripción
            Pull Request automático para mergear cambios de la rama `pre_prod` a `main`.
            
            ## 🔄 Cambios incluidos
            Este PR incluye todos los cambios que están en `pre_prod` y no están en `main`.
            
            ## 🧾 Detalles técnicos
            - Rama base: `main`
            - Rama origen: `pre_prod`
            - Rama temporal: `${{ env.PR_BRANCH }}`
            
            ## ✅ Checklist
            - [ ] Revisar los cambios incluidos
            - [ ] Verificar que no hay conflictos
            - [ ] Aprobar el merge si todo está correcto
            
            ---
            *PR generado automáticamente por GitHub Actions*
          reviewers: ${{ github.actor }}
          labels: |
            automated-pr
            pre-prod-to-main
            merge

      - name: Verify pull request creation
        if: env.DIFF_EXISTS == 'true'
        run: |
          echo "✅ Pull Request creado exitosamente desde la rama ${{ env.PR_BRANCH }} a main"
          echo "La rama temporal permanecerá hasta que el PR sea mergeado"

      - name: No changes needed
        if: env.DIFF_EXISTS == 'false'
        run: |
          echo "ℹ️ No se creó Pull Request porque no hay diferencias entre main y pre_prod."
          echo "✅ El repositorio $REPO_NAME está actualizado." 